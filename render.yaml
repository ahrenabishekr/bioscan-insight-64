services:
  - type: web
    name: chemosense-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server-render.js
    envVars:
      - key: NODE_ENV
        value: production
